name: CI

on:
  push:
    branches: [ main, dev, feature/* ]
  pull_request:
    branches: [ main, dev ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run linting
      run: npm run lint

    - name: Run tests
      run: npm test

    - name: Upload test coverage
      uses: actions/upload-artifact@v3
      with:
        name: coverage
        path: coverage/
        if-no-files-found: error

# Remove build job until EAS Build is configured
# build:
#   needs: test
#   runs-on: ubuntu-latest
#   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev'
#
#   steps:
#   - uses: actions/checkout@v3
#
#   - name: Use Node.js
#     uses: actions/setup-node@v3
#     with:
#       node-version: '18.x'
#       cache: 'npm'
#
#   - name: Install dependencies
#     run: npm ci
#
#   - name: Build app
#     run: npm run build
#
#   - name: Upload build artifacts
#     uses: actions/upload-artifact@v3
#     with:
#       name: build
#       path: build/
#       if-no-files-found: error 